import Button from '@mui/material/Button'

const MUI = () => {
  return (
    <>
       <Button variant="text" color="primary">
         Hello World
       </Button>
    </>
  );
};

export default MUI;